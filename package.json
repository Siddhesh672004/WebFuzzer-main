{
  "name": "smartfuzz",
  "version": "0.1.0",
  "private": true,
  "description": "SmartFuzz — zero-cost, fully local, intelligent web vulnerability scanner",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "backend",
    "worker",
    "frontend",
    "payloads"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:shared": "npm run test --workspace shared",
    "test:backend": "npm run test --workspace backend",
    "test:worker": "npm run test --workspace worker",
    "test:frontend": "npm run test --workspace frontend",
    "lint": "npm run lint --workspaces --if-present",
    "dev:backend": "node --env-file=.env --watch backend/src/server.js",
    "dev:worker": "node --env-file=.env --watch worker/src/index.js",
    "dev:frontend": "npm run dev --workspace frontend",
    "seed": "node --env-file=.env payloads/seed.js",
    "setup:payloads": "node --env-file=.env payloads/setup.js"
  }
}
